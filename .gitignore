# Claude Code / local tooling
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Dependencies (none currently, but just in case)
node_modules/

# original full-size images (kept locally, not deployed)
assets/originals/
